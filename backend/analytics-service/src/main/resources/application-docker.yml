server:
  port: 8087

spring:
  config:
    import: optional:configserver:http://config-server:8888

  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/ecobridge_analytics
    username: postgres
    password: divyansh123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true



eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/

