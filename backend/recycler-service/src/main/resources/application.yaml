spring:
  application:
    name: recycler-service

  profiles:
    active: dev


  config:
    import: ${SPRING_CONFIG_IMPORT:optional:configserver:http://config-server:8888}

management:
  endpoints:
    web:
      exposure:
        include: "*"