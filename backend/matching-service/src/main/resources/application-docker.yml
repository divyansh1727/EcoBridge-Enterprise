server:
  port: 8086

spring:

  config:
    import: optional:configserver:http://config-server:8888

  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/ecobridge_matching
    username: postgres
    password: divyansh123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

security:
  jwt:
    secret: 1234567890123456789012345678901234567890123456789012345678901234

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"