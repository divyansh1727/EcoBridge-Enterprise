server:
  port: 8081

spring:

  config:
    import: optional:configserver:http://config-server:8888

  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/ecobridge_auth
    username: postgres
    password: divyansh123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

security:
  jwt:
    secret: ${JWT_SECRET}

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"