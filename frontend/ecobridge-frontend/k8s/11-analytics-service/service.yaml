apiVersion: v1
kind: Service

metadata:
  name: analytics-service
  namespace: ecobridge

spec:
  selector:
    app: analytics-service

  ports:
    - port: 8087
      targetPort: 8087

  type: ClusterIP