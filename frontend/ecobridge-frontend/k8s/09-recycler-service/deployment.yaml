apiVersion: apps/v1
kind: Deployment

metadata:
  name: recycler-service
  namespace: ecobridge

spec:
  replicas: 1

  selector:
    matchLabels:
      app: recycler-service

  template:
    metadata:
      labels:
        app: recycler-service

    spec:
      containers:
        - name: recycler-service
          image: ecobridge/recycler-service:1.0

          ports:
            - containerPort: 8085

          envFrom:
            - configMapRef:
                name: ecobridge-config

            - secretRef:
                name: ecobridge-secret

          env:
            - name: SPRING_CONFIG_IMPORT
              value: "optional:configserver:http://config-server:8888"

            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres:5432/ecobridge_recycler"

            - name: EUREKA_SERVER_URL
              value: "http://discovery-server:8761/eureka/"