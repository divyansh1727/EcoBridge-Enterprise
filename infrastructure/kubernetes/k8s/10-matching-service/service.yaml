apiVersion: v1
kind: Service

metadata:
  name: matching-service
  namespace: ecobridge

spec:
  selector:
    app: matching-service

  ports:
    - port: 8086
      targetPort: 8086

  type: ClusterIP